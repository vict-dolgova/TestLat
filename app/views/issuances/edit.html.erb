<h1>Продление выдачи</h1>
<p>
  <strong>Книга</strong>
  <%= Book.find(@issuance.book_id).cipher %> <%= Book.find(@issuance.book_id).author %> <%= Book.find(@issuance.book_id).name%>
</p>
<p>
  <strong>Абонент</strong>
  <%= Subscriber.find(@issuance.subscriber_id).ticketnumber %> <%= Subscriber.find(@issuance.subscriber_id).surname %> <%= Subscriber.find(@issuance.subscriber_id).name%>
</p>
<p>
  <strong>Дата выдачи</strong>
  <%= @issuance.issueDate%>
</p>
<%= render 'form', issuance: @issuance %>
